schema_version = 1
name = "tofu-fmt"
commands = ["tofu fmt"]

[[stages]]
strategy = "ansi_strip"

[[stages]]
strategy = "regex_capture"
pattern = "(?s)(.+)"
format = "$1"
fallback = "fmt: ok"

[[tests]]
id = "empty-ok"
input = ""
expected = "fmt: ok"
